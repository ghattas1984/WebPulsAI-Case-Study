Crafted for WebPulse extractor to hit target features:
- Broken Links: 13 absolute links to missing paths on example.com
- Number of Requests: 184 tags (<script>=120 + <link>=64)
- Target HTML size ≈ 0.2188 MB to yield compression ≈ 89 KB with formula int(page_size_MB*1024*0.4)
- One minimal HTML validation error: unclosed <main>
- Time metrics depend on hosting/network and cannot be forced from static HTML.
